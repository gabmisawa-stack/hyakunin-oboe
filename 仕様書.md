# ふだっち — 百人一首おぼえアプリ 仕様書

版：2026-08-31.r10（公開ずみ。使う人の切り替えを追加、「はやい」の基準を実音源に合わせた）

**公開URL：https://gabmisawa-stack.github.io/hyakunin-oboe/**
リポジトリ：https://github.com/gabmisawa-stack/hyakunin-oboe （public）
作成：Claude Code ／ 依頼：三澤尚久

---

## 1. 目的と利用者

三澤なつ（小4）が、学校の競技かるたに向けて百人一首を覚えるためのアプリ。
本人の希望は3つ。

1. **決まり字を覚えたい**
2. **百人一首独特の読み方を聞きながら練習したい**
3. 五色百人一首（手元にある）の**色別に覚えたい**

加えて、なお先生からの要件が2つ。

4. **覚えていない札を重点的に出題する**
5. 文字だけでなく、**札が並んでタップするゲーム的な操作**にする

想定利用シーン：家のiPadで1日10〜15分。1セッション＝20首（五色の1色ぶん＝実際の一試合と同じ枚数）。

将来的に晴・華も使う可能性があるので、プロフィール分離は設計に含める（実装はv1.2）。

---

## 2. 動作環境

- **iPad / Safari**。ホーム画面に追加して全画面（standalone）で使う
- 横向き（landscape）を基本とする。札を並べるため
- **GitHub Pages で公開＋PWAでオフライン動作**
- ビルド不要。バニラJS＋単一HTML＋JSON。依存ライブラリなし

リポジトリ名は **`hyakunin-oboe`**（`kotowaza-oboe` に揃えた）。`gabmisawa-stack` アカウントに作る。

> 依存ゼロにする理由：iPadだけで完結し、npmもNode.jsも要らず、10年後もそのまま開ける。

### 2.1 iOS Safari 特有の注意（実装時に効く）

| 制約 | 対処 |
|---|---|
| 音声はユーザー操作を起点にしないと鳴らない | 最初の「はじめる」タップで無音を1回再生して unlock する |
| ダブルタップでズームしてしまう | `touch-action: manipulation`、`user-scalable=no` |
| 長押しで文字選択・コピーメニューが出る | 札に `user-select: none`、`-webkit-touch-callout: none` |
| ホーム画面PWAのストレージがITPで消える可能性 | **未検証**。保険として学習記録のJSONエクスポートを付ける（§7.4） |
| ゴムバンド・スクロールで画面が動く | `overscroll-behavior: none`、盤面は固定レイアウト |

---

## 3. データ設計

### 3.1 歌データ `data/poems.json`

100首。1首あたりのフィールド：

```json
{
  "id": 1,
  "kaminoku": "秋の田の かりほの庵の 苫をあらみ",
  "shimonoku": "わが衣手は 露にぬれつつ",
  "kaminoku_kana": "あきのたのかりほのいほのとまをあらみ",
  "shimonoku_kana": "わがころもではつゆにぬれつつ",
  "kaminoku_ruby": "秋[あき]の田[た]の かりほの庵[いほ]の 苫[とま]をあらみ",
  "author": "天智天皇",
  "author_kana": "てんぢてんのう",
  "kimariji": "あきの",
  "kimariji_len": 3
}
```

- `*_kana` は**決まり字の判定に使う正規化かな**。歴史的仮名遣いの**表記**で持つ（「いほ」であって「いお」ではない）。決まり字は読み札の字面で決まるため
- `*_ruby` は児童向けのふりがな表示用。`漢字[よみ]` 形式で持ち、描画時に `<ruby>` に変換する

### 3.2 決まり字は「持たない」——算出する ★設計判断

決まり字を人手で入力すると、必ずどこかで間違える。**100首の `kaminoku_kana` から機械的に算出**する。

> 決まり字＝その歌の上の句の先頭から、**他の99首のどれとも一致しなくなる最短の文字列**

★ただし比較は**字面ではなく「音」**で行う。ここを間違えると必ずズレる（実際に2回踏んだ）。

| 正規化 | 例 | これを怠ると |
|---|---|---|
| を→お ゐ→い ゑ→え ぢ→じ づ→ず | 26番「をぐらやま」＝おぐらやま | 「を」が1字決まりになり、1字決まりが8首になる |
| 語頭の あふ→おお、おほ→おお（長音「オー」の同一視） | 44番「あふことの」＝おーことの<br>60番「おほえやま」＝おーえやま<br>95番「おほけなく」＝おーけなく | 44番が2字決まりになり、2字43首・3字36首と定説からずれる |

`kaminoku_kana`（札の字面）は原文のまま保存し、比較用の読み `kaminoku_yomi` を別に持つ。
決まり字は**字面（`kimariji`）と読み（`kimariji_yomi`）の両方**を出す。札には「をぐ」と書いてあるが、耳に届くのは「おぐ」。アプリはどちらも使う。

ビルド時（というより準備スクリプト）に算出し、`poems.json` に焼き込む。実行時に再計算はしない。

**自己検証テスト**（`tools/build.mjs`。全て実装済み・パス）：

| # | 検証項目 | 期待値 | 結果 |
|---|---|---|---|
| 1 | 1字決まり | ちょうど7首。頭音が **む・す・め・ふ・さ・ほ・せ** | ✅ す18 ふ22 め57 さ70 せ77 ほ81 む87 |
| 2 | 6字決まり | 「あさぼらけあ」「あさぼらけう」を含む | ✅ |
| 3 | 全首 | 決まり字が字面で重複しない | ✅ |
| 4 | 全首 | 決まり字が上の句の先頭部分になっている | ✅ |
| 6 | 全首 | 決まり字が**読み**でも重複しない | ✅ |
| 7 | 字数分布 | 1字7／2字42／3字37／4字6／5字2／6字6 | ✅ 完全一致 |
| 8 | 頭音の五十音分布 | 文化ネットの27区分（あ16 い3 う2 お7 …）と一致 | ✅ 27区分すべて一致 |

**外部の公表データとの全件照合**（`tools/crosscheck.mjs`。参照は[文化ネット 五色分類](https://www.bunkanet.jp/manabi/hyakunin-isshu-karuta/gosyoku/)、2026-08-31取得）

集計値の一致だけでは足りないので、100首を1件ずつ突き合わせた。

| 照合項目 | 結果 |
|---|---|
| 決まり字（音で比較） | ✅ **100/100 一致** |
| 決まり字の文字数 | ✅ **100/100 一致** |
| 取札＝下の句（清音で比較） | 98/100（相違2件はいずれも先方の脱字。下記） |
| 五色の色分け | ✅ **100/100 一致** |

比較にあたっては、ハ行転呼（は/わ・ひ/い・ふ/う・へ/え・ほ/お）と旧仮名（を/ゐ/ゑ/ぢ/づ）と む/ん を同一視する。
文化ネットは決まり字を**読み**で書き（62番「よお」、50番「きみがためお」、44番「おおこ」）、
札の字面は「よを」「きみがためを」「あふこ」。**読みの側が完全一致した**ので、§3.2の正規化規則は正しい。

⚠️ 相違2件は当方の誤りではない。文化ネットの「取札」列が、**同ページの「下の句の読み」列と食い違っている**：

| 番号 | 当方（実物カード） | 文化ネット「取札」 | 文化ネット「下の句の読み」 |
|---|---|---|---|
| 29 | おきまど**は**せるしらぎくのはな | おきまとせる… | おきまど**わ**せる…（は有り） |
| 55 | なこそながれてなほ**き**こえけれ | …なほこえけれ | …なお**き**こえけれ（き有り） |

いずれも先方の取札列の脱字。原歌「置きまどはせる」「なほ聞こえけれ」からも当方が正しい。

→ **歌データ100首は検証済みとする。**再照合は `tools/fetch-ref.sh && node tools/crosscheck.mjs`。

### 3.3 五色データ `data/goshoku.json`

**確定済み。** `data/goshoku.json` に格納。

- **正は手元の実物カード**（2026-08-31 撮影 IMG_1092〜1096）。取り札20枚を色ごとに並べて撮影し、**下の句から歌番号に引き当てた**。下の句は100首すべて異なるので、引き当てに解釈の余地はない
- **アプリの表示は学校での呼び方**「**青・赤・オレンジ・緑・黄色**」を使う（なつさんが耳で聞く言葉に合わせる）。
  データには協会の公式表記「青・桃・黄・緑・橙」も `label_official` として持ち、外部資料との照合に使う
- 検証：5色×20＝100、重複なし、1〜100が過不足なく1回ずつ ✅

**外部との突き合わせ（2026-08-31）**

| 照合先 | 結果 |
|---|---|
| [文化ネット 五色分類一覧](https://www.bunkanet.jp/manabi/hyakunin-isshu-karuta/gosyoku/) | ✅ **全100首・5色すべて完全一致**（歌番号つきで公開されている） |
| [honda-n2 五色百人一首一覧](https://honda-n2.com/gosyokuhyakuninisshu-ichiran) | 95首一致。残り5首は先方の表記の問題（下記） |

なお、文化ネットは**取札（下の句）の字面も公開している**。実物カードの写真から読み取った下の句と
突き合わせた結果 98/100 が一致し、相違2件は先方の脱字だった（§3.2）。
つまり **「写真の読み取り→歌番号の引き当て」自体が外部データで裏を取れている**。

honda-n2 との相違5件は、こちらの誤りではない：

- 「わたのはら」「きみがため」が各2首あるのを **第一／第二** とだけ書いて歌番号を出しておらず、どちらを指すか確定できない（4件）。実物カードは下の句が違うので一意に決まる。文化ネットも実物と一致
- 「ひともをし」が**緑と橙の両方に載っている**（1件）。緑の正しい札は「ひとはいさ」＝35番。実物カード・文化ネットとも35番

⚠️ **決まり字の表記が資料によって違う**。文化ネットは読み（62番「よお」、50番「きみがためお」）、札の字面は「よを」「きみがためを」。本アプリは両方持つので、表示の切り替えで対応する。

### 3.4 学習記録（端末内）

`localStorage` にプロフィールごとに保存。1首あたり：

```json
{ "id": 1, "box": 2, "seen": 7, "correct": 5, "lastSeen": 1756... , "fastest": 1830, "recent": [1,1,0,1,1] }
```

- `box`：0〜5のライトナー箱（§7）
- `fastest`：最速の反応時間（ms）
- `recent`：直近5回の正誤

---

## 4. 音声設計 ★このアプリで一番ややこしいところ

### 4.0 判断の分かれ目は「配布するか」ではなく「公開サーバーに置くか」

このアプリは三澤家の中だけで使う。それでも**音源をリポジトリに入れることはできない**。
理由は「配布しないから大丈夫」が成り立たないため。効いてくる条文が2つあり、家庭内利用が効くのは片方だけ。

| 行為 | 条文 | 家庭内利用なら |
|---|---|---|
| **複製**（入手した音源を家のiPadに入れる） | 著作権法30条1項（私的使用のための複製） | ✅ **問題ない** |
| **公衆送信・送信可能化**（GitHub Pages に置く） | 同23条。**30条の対象外** | ❌ URLを誰にも教えなくても不可 |

GitHub Pages は無料プランではアクセス制限をかけられない。リンクを公表していないURLも、
実務上は「不特定の者に対する送信可能化」として扱われる。**家族しか使っていないという事実は効かない。**

→ したがって §4.2 の三層構成と §4.3 の取り込みUIは、制約を避けるための遠回りではなく、
**やりたいこと（本物の読み方でなつさんが練習する）を合法に実現する最短経路**である。

なお「サイトを公開しない」なら音源を同梱できるが、`file://` では Service Worker が動かず、
**ホーム画面追加とオフラインPWAを失う**（§2）。毎回ファイルを転送し直すことにもなるので採らない。

### 4.1 なぜ音源をリポジトリに入れないか

無料で入手できる読み上げ音源は、調べた範囲ではすべて**再配布の許諾がない**。

| 入手先 | 位置づけ | 再配布 |
|---|---|---|
| 小倉百人一首 音声つき（ogura100.roudokus.com） | メルマガ読者サービス。全100首＋序歌のmp3。メール登録制 | 記載なし。`(C) 2012 Sirdaizine Inc.` のみ |
| 百人一首ワンダーランド（東京書籍） | 書籍購入者向けダウンロード | 不可と解すべき |
| 奇跡の百人一首（祥伝社） | 書籍購入者向け。著者朗読 | 不可と解すべき |
| 全日本かるた協会 専任読手の音源 | 協会の著作物 | 不可 |
| **[LibriVox "Ogura Hyakunin Isshu"](https://archive.org/details/hyakunin_isshu_librivox)**（2006・朗読 kaseumin） | **パブリックドメイン**（`creativecommons.org/publicdomain/mark/1.0/`。LibriVoxは全録音をPD提供） | ✅ **可** |

GitHub Pages への同梱は**公開＝再配布**にあたるので、LibriVox以外は使えない。

### 4.1.1 LibriVox版パブリックドメイン音源の実測（2026-08-31）

唯一同梱できる音源。ただし**競技かるたの読み方ではない**ので、位置づけを間違えないこと。

| 項目 | 実測値 |
|---|---|
| ファイル構成 | 10ファイル（10首ずつ）。64kbps MP3・22.05kHz・モノラル相当 |
| 総容量／総時間 | 約15MB ／ 32分47秒 |
| 1ファイルの中身 | LibriVoxの英語イントロ（約12秒）＋タイトル読み上げ＋10首 |
| 1首あたり | 通し読み**約7秒** ＋ 作者名 約1.2秒。歌と歌の間は約5秒の無音 |
| 分割 | 無音検出（−40dB・4秒以上）で機械的に切れる。100ファイル化は自動化できる |

⚠️ **限界**。7秒の通し読みで、**上の句と下の句の間に「間」がない**。序歌も節回しもない。
つまり、なつさんが言った「百人一首独特の読み方」は、この音源では手に入らない。
モード③④の練習や、音源を入れる前の初期状態としては十分に使える。

（CCライセンスの競技かるた式音源も探したが、見つからなかった。
[わすらもち](https://pjmtdw.github.io/wasuramoti/)の「難波津いなばくん」等は
**作者の許諾を得て同梱**しているもので、こちらが再配布することはできない。）

### 4.2 解決：音源プラガブル設計

アプリ本体と音源を分離する。**リポジトリには音源を一切含めない。**

```
AudioSource（インターフェース）
  ├─ FileSource     … 取り込んだmp3。IndexedDBに保存。端末内のみ。★最優先
  ├─ LibriVoxSource … パブリックドメイン音源。リポジトリに同梱してよい唯一の実声
  └─ SynthSource    … iPadの日本語読み上げ（Web Speech API）。最終手段
```

起動時に上から順に使えるものを選ぶ。**音源を1つも入れなくても全モードが動く**。

- リポジトリに同梱するのは **LibriVox（PD）だけ**。これで公開しても再配布の問題が起きない
- 本物の読み方が欲しければ、なお先生が個人的に入手した音源を iPad から取り込む（§4.3）。
  取り込んだ音源は端末内にしか保存されないので、私的利用の範囲に収まる

### 4.3 音源の取り込みUI

`設定 › 読み上げ音声 › 音源を取り込む`

1. `<input type="file" multiple accept="audio/*">` でmp3を複数選択
2. ファイル名の先頭の数字を歌番号として読む（`001.mp3` `01_秋の田の.mp3` 等）
3. **番号が読めない／重複するときは、名前順に1番から割り当てるか聞く**。
   配布元によってファイル名がまちまちなので、逃げ道を必ず用意する。
   序歌が混ざると1首ずつずれるので、その旨も出す
4. IndexedDB に Blob で保存。100首×約300KB＝**目安30〜60MB**
5. 取り込み済みの首数を設定画面に表示。全削除もできる
6. **`音源を たしかめる`** で100首を順に鳴らし、歌と音がずれていないか確認できる。
   取り込み直後は必ずここを通す

> 序歌があれば `id: 0` として同じ仕組みで取り込む。セッション開始時に鳴らす。

### 4.4 再生の仕様

- **1ファイル＝1首まるごと**（上の句＋下の句）を前提とする
- 目標は「**下の句が読まれる前にタップする**」。これが競技かるたの練習になる
- 正解をタップした時点で再生を止めるかどうかは設定で選べる（止める＝テンポ重視／最後まで聞く＝耳で覚える）
- v1.5：前の歌の下の句 → 次の歌の上の句 を繋いで読む「連続読み」。実際の試合の耳が作れるが、実装は後回し

### 4.6 入手した本物の音源（Hyaku_Setup03）✅ 検証ずみ 2026-08-31

`~/Downloads/Hyaku_Setup03/`。**これがなつさんの言う「独特の読み方」の音源。**

| 項目 | 実測 |
|---|---|
| 構成 | mp3 100ファイル。**序歌なし・100首ちょうど**（ずれる心配がない） |
| ファイル名 | `001 秋の田のかりほの庵の… （天智天皇）　朗詠.mp3` |
| 1首の長さ | **24〜25秒**。LibriVoxの7秒に対し3倍以上。節回しのある朗詠 |
| 音質 | 320kbps・44.1kHz・ステレオ。合計 **94MB** |

**番号と歌の対応は全100首正しい**。ファイル名の作者名100件を `poems.json` と照合して
**100/100 一致**（`~/Downloads/百人一首音声/`）。

⚠️ **展開のしかたで結果が変わる。** 最初に見たフォルダでは4件（曾禰好忠・大中臣能宣朝臣・
能因法師・前中納言匡房）の作者名が化けていた。zipのファイル名が **Shift-JIS** なので、
展開ツールを誤ると「能」が脱落したり 前→権 に化けたりする。
`unzip` は `Illegal byte sequence` で失敗する。**Finder で展開すること。**
（コマンドから展開するなら Python の `zipfile` で `cp932` を指定する）

⚠️ 歌本文どうしの機械照合は**できない**。出版元と当方で漢字/かなの表記の慣習が違うため
（「我がころも手は」vs「わが衣手は」、「寝む」vs「ねむ」）。作者名での照合が有効だった。

ファイル名が `001 ` で始まるので、取り込みUIは先頭の数字をそのまま歌番号として読める。**名前順フォールバックは不要。**

**⚠️ 94MB は iPad に入れるには重い。** 話し声なので `~/Downloads/百人一首音声_iPad用/` に
**モノラル64kbps AAC** で作り直した版を置いた。ファイル名は元のまま（先頭の番号も残る）。
取り込むならこちらを推奨。**どちらもリポジトリには入れない**（§4.0）。

### 4.5 合成音声の限界を隠さない

Web Speech API の日本語音声は、序歌の節回しも上の句の引き伸ばしも再現できない。カーナビの音声に近い。
初回起動時に一言だけ出す：「いまは iPad の声で読んでいます。ほんものの読み方の声は、あとから入れられます」

---

## 4.7 参考にした先行アプリ「読上げ・五色百人一首」（ADCL 2017）

https://t-asami.sakura.ne.jp/mobile/ogura/ （勤務先の児童から教わったもの）

読手の代わりをするアプリ。札は現物を使う。中身を読んで分かったこと：

| 設定 | 中身 |
|---|---|
| **札色（複数可）** | 5色を自由に組み合わせる。2色で40首、3色で60首 |
| 読み上げ句 | **上句のみ 9秒 ／ 下句のみ 10.6秒 ／ 上句＋下句 20.5秒 ／ 上句＋下句＋下句 35秒**（実測）。4種類の音源を別々に持っている |
| 札表示 | 非表示／下札／上札／全表示。画面に札の画像を出す |

★**「上・下句2」は下の句を2回読む形。**教室で子どもに取らせるとき、下句をもう一度読んで確かめる作法。

★ このサイトの `hyaku1.js` は**100首の色分けを持っている**。当方のデータと照合して**100/100 一致**。
実物カード・文化ネットに続く**3つ目の独立資料**での確認になった。

**取り入れたもの**：色の複数選択（下記）。
**取り入れていないもの**：読み上げ句の選び分け（当方の音源は上句と下句が1ファイルで、切れ目のデータがない）。

## 4.8 他の練習サイトの調査（2026-09-01）

| サイト | 参考にしたところ |
|---|---|
| [百人一首.jp 練習ツール](https://hyakuninisshu.jp/practice/) | 8モード。**耳トレ＝反応時間をミリ秒で計測し決まり字別のヒートマップ**、タイムトライアル＝1/3/5分で何首取れるか、散らし練習＝100首ランダム |
| [時雨の百人一首](https://hyakuninisshu.sakura.ne.jp/karuta.html) | **取り終えるまでのタイムを出し、お手つき1回につき5秒加算**。この加点方式をそのまま採った |
| [全日本かるた協会](https://www.karuta.or.jp/karuta/rule/) | 本式は50枚を25枚ずつ、**15分の暗記時間**、空札、送り札、自陣が先に無くなれば勝ち |
| 五色百人一首（学校） | 20枚を2人で10枚ずつ、**3〜5分で決着** |

## 4.9 音源を上の句・下の句に切り分ける

`tools/split-yomi.py`。朗詠の1ファイル（24〜26秒）を無音検出で切る。

**実測**：99/100首が発話3区間。区間1 平均6.2秒／区間2 7.0秒／区間3 7.3秒。
区間2と3の相関 0.61。**「上の句 → 下の句 → 下の句（くり返し）」**の構成と整合し、
ADCLサイトの「上・下句２」も同じ形だった。

⚠️ **区間1が上の句だという確証はない。** 字数との相関はゼロ（朗詠は節回しで長さが決まるため）、
区間内の句の切れ目でも判別できなかった（切れ目なく歌っている）。**耳で確かめるしかない。**
そのため設定に**「上下を いれかえる」**を置いた。逆だった場合はそこで直せる。

⚠️ 9番だけ区間2と3の間が0.44秒しかなく、既定の0.5秒では繋がってしまった。
足りなければ間の判定を0.4→0.35→0.3秒と下げて取り直すようにしてある。

出力は `NNN_kami.m4a` / `NNN_shimo.m4a`（100首・200ファイル・12MB）。
取り込みUIはファイル名の `kami`/`shimo`（`上`/`下`でも可）を見て分ける。

## 4.10 モード⑤ 試合

五色百人一首の一試合をひとりでやる形。

1. **暗記タイム** — 20枚を並べ、めくって上の句を見られる。0/30秒/1分/2分から選ぶ（競技かるたは15分）
2. **試合** — 上の句を連続で読む。取ると札が消える。**お手つきは +5秒**（時雨の百人一首の流儀）
3. **結果** — タイム・お手つき・自己ベストとの差。ベストは `色の組み合わせ:枚数` ごとに残す

- **空札**を混ぜる設定あり。盤にない歌もわざと読む。取ってはいけない札を我慢する練習
- 分割音源があると上の句（約6秒）だけを読むので、本番と同じ速さになる。無いと1首24秒かかる
- ⚠️ **進行は100msの時計ひとつで駆動する。** 最初 setTimeout を積む作りにしたら、
  音の終わり・時間切れ・札を取った の3つが競合して試合が止まった。
  音が鳴らない環境でも15秒で次へ進む

## 4.11 BGM（Suno製・和のインスト）

読み上げのないモード（決まり字クイズ・上の句→下の句・暗唱チェック）とホーム画面で流す。

**曲は画面ごとに1曲ずつ、きっちり固定する。**音を聞けばどの画面かが分かる。

| 画面 | 曲 |
|---|---|
| ホーム | koto-morning-a（箏ソロ60） |
| 決まり字クイズ | paper-lantern-a（箏・三味線85。動きがある＝速さを競う場面） |
| 上の句→下の句 | breath-of-pine-a（尺八70。じっくり選ぶ） |
| 暗唱チェック | court-silence-a（笙・篳篥50。静かに思い出す） |
| 結果 | paper-lantern-b（85で明るい。やり切ったあと） |
| せいせき | breath-of-pine-b（しっとり。記録を振り返る） |
| せってい | koto-morning-b（箏ソロ。淡々と） |

⚠️ 最初「a/bは同じ系統の予備」と考えて、結果・せいせき・せっていを他と共用にし、
「3系統たりない」と判断した。**まちがい。**a/bは同じ楽器・テンポでも別の演奏なので、
**7曲あって画面も7つ、1対1でちょうど足りる**。追加生成は要らなかった。
曲は `el.loop = true` でそのまま繰り返す（画面が変わらないかぎり曲も変えない）。
読み上げが鳴るモード（札取り・試合・はじめておぼえる）では**止める**。声とぶつかるため。

Sunoで作成（Proプラン・v5.5・全曲インスト）。プロンプトは
`Suno音源バンク_引継書.md` の作法に従い **②神曲（ボーカルなし）GPTs** に書かせた。
「4軸（編成・テンポ・明暗・ジャンル×年代）が1つも重複しないように」と指示。

| 曲 | 編成 | テンポ | 明暗 |
|---|---|---|---|
| koto-morning a/b | 箏ソロ | 60 | 長調のやわらかさ |
| breath-of-pine a/b | 尺八＋薄いパッド | 70 | 短調のしっとり |
| paper-lantern a/b | 箏・三味線・軽い打楽器 | 85 | 長調＋20年代ローファイ |
| court-silence a | 笙・篳篥のドローン | 50 | 中性（雅楽ふう） |

計7曲・12MB（モノラルではなくステレオ96kbps AAC。音楽なので）。1生成=2曲で4生成、**40クレジット**。

⚠️ **Sunoの「M4A」でダウンロードすると中身が Opus** で、iPad Safariで再生できない。
引き継ぎ書が **MP3** を指定していたのはこのため。踏んでから気づいた。**MP3で落とすこと。**
⚠️ 8曲のうち1曲が0:11だった（引き継ぎ書の「30秒未満を弾く」に該当）。自動で除外している。

### さいしょの画面（名前をきく）

だれも登録されていない端末では、**名前をきく画面から始まる**。
アプリを配る相手が三澤家とはかぎらないので、「なつ」を既定にしない。

⚠️ **すでに使っている端末を、この画面に戻してはいけない。**
`fudacchi.profiles` が無くても、`fudacchi.progress.<名前>` が残っていれば
そこから名前を拾って登録し直す（`migrate()`）。なつさんの記録はそのまま続く。

### ミュートボタン

画面の右下に出しっぱなしにする。押せば即、**BGMも読み上げも**止まる。
設定画面まで行かせない（「面倒だから」という理由で置き場所を変えた）。

⚠️ 読み上げは「鳴らさない」のではなく **`muted` で無音のまま再生する**。
止めてしまうと、音の終わりを合図に進む試合モードが止まる。

## 5. 画面構成

```
ホーム
 ├─ 色をえらぶ … 赤／青／黄色／緑／オレンジ（★複数えらべる）／ぜんぶ／にがてだけ
 │    五色百人一首は「青を覚えたら緑を足す」と範囲を広げていく教え方なので、
 │    20首→40首→60首と積み上げられるようにした。選んだ合計首数を見出しに出す
 ├─ モードをえらぶ … ①札取り ②決まり字クイズ ③上の句→下の句 ④暗唱チェック
 ├─ せいせき … 色ごとの習得率、にがて札の一覧、これまでの記録
 └─ せってい … 音源、ふりがな、札の枚数、音の速さ、プロフィール
```

セッションは「色 × モード」で始まり、20問（設定で10/20/全部）で終わる。終了後に結果画面。

---

## 6. 練習モード（5つ）

### モード⓪ はじめて おぼえる —— 何も知らない人はここから

⚠️ **設計の穴だった。**当初の4モードは**全部「テスト」で、「教える」ものが1つもなかった。**
何も知らない子に上の句を見せても下の句は当てずっぽうにしかならず、4択なら25%。
当たっても身につかず、外し続ければ全首が箱0に落ちて出題が偏るだけになる。

1回あたり5首（`OBOE_BATCH`）を、**8だんかい**で回す。

| lv | 形 | 中身 |
|---|---|---|
| 0 | みる | 上の句・下の句をかなと漢字で並べ、決まり字を金で示す。作者名と「『しの』まで きこえたら」も添える。音も鳴る。**採点しない** |
| 1〜4 | 1〜4もじ | **下の句のあたまを1・2・3・4文字かくし**、かなを順にえらんで埋める |
| 5〜7 | 2択・3択・4択 | 下の句を選ぶ。選択肢は**いま覚えている5首のなかから**だけ |

**同じ歌を、少しずつ形を変えて何度も出す（変化のある繰り返し）。**
1つの形だけ繰り返しても、その形に慣れるだけで身につかない。

**出す順の決め方**（エビングハウスの忘却曲線）：

- **見せた直後に、間をあけずにその歌を試す。**直後の反復がいちばん残る
- そのあとは、いちばん進んでいない歌から出す。5首を回すので**間隔が自然に開いていく**
- **まちがえたら1つ下げて、すぐもう一度**。忘れかけた直後に取り出すのが効く
- すらすら答えた（しきい値より速い）ら**1つ飛ばす**。分かっている歌に8回つきあわせない

到達レベルは `P[id].lv` に残るので、**日をまたいでも続きから**始まる。
1セッションは最大44手（`OBOE_MAX_STEP`）で打ち切る。

- 5首という数は、覚える前に忘れない範囲として置いた暫定値。使ってみて調整する
- 「にがてだけ」と組み合わせると、落ちた札の入れ直しにも使える

## 6.1 練習モード（テストする4つ）

### モード① 札取り —— メイン

競技かるた本番に一番近い形。

**読む前に、札をめくって覚えられる。** 五色百人一首は**取り札の裏に上の句と作者名が刷ってあり、
試合の合間にそれを見て暗記する**。向山洋一が1990年の商品化のときに入れた仕組みで、
1989年に小島光雄から得た「20枚ずつに分ける」発想に、これを足したもの
（[向山洋一公式サイト](https://mukoyamayoichi.com/works/work05)）。

アプリでも同じにした。出題されると、まず盤と「よみはじめる」だけが出る。
札をタップすると裏返り、上の句（決まり字は金）と作者名が見える。
「よみはじめる」を押すと全部表に戻り、読み上げと時計が始まる。
設定`よむ まえに めくれる`で切れる（本番に近づけたいとき）。

- 上の句が**読み上げられる**（音のみ。文字は出さない）
- 画面下に**取り札（下の句）が並ぶ**。4／8／16枚から選択。初期値8枚
- 正しい札をタップ → 札が光って消える、次の歌へ
- お手つき → 赤く震え、正解の札が一瞬光る。その歌は box 0 に落ちる
- 上部に**経過時間バー**。下の句が読まれ始める瞬間に印が出る。ここより手前で取れたら「はやい！」
- 誤答の選択肢は**ランダムではなく、決まり字が近い札を優先**して混ぜる。「あさぼらけ」の2首、「わたの原」の2首のような、本番で迷う組を意図的にぶつける
- 札は**縦書き**（`writing-mode: vertical-rl`）。実物の札に近づける

### モード② 決まり字クイズ

- 決まり字だけが1文字ずつ現れる（「あ」→「あき」→「あきの」）
- 何文字目で分かったかを記録する。**早く押せたほど高評価**
- 決まり字が確定した瞬間に画面の色が変わる。「ここまで聞けば分かる」を体で覚える
- 1字決まり7首（む・す・め・ふ・さ・ほ・せ）は専用の特訓メニューを別に用意する

### モード③ 上の句 → 下の句マッチング

- 上の句を**文字で**見て、下の句を選ぶ（4択）。選択肢にも**かなを添える**（漢字だけだと小4には読めない）
- 音を出さずにできる静かなモード。寝る前や車の中向け
- ふりがな表示のオン・オフあり

### モード④ 暗唱チェック

- 上の句だけ出て、下の句は隠れている
- 頭の中で唱えてから「見る」をタップ → 答え合わせ → 「言えた／言えなかった」を自己申告
- **自己申告なので記録の精度は本人の正直さ次第**。①〜③の記録とは別枠で持ち、出題アルゴリズムへの重みは低くする

---

## 7. 「覚えていないものを重点的に」の出題アルゴリズム

### 7.1 ライトナーの箱（box 0〜5）

| box | 意味 | 次に出るまでの目安 |
|---|---|---|
| 0 | 未習得・間違えた | 同じセッション内で再出題 |
| 1 | 1回できた | 次のセッション |
| 2 | 2回できた | 1日後 |
| 3 |  | 3日後 |
| 4 |  | 1週間後 |
| 5 | 習得 | 2週間後 |

### 7.2 速さも見る ★競技かるた向けの工夫

正解しても**遅ければ箱を上げない**。かるたは速さが勝負なので、「分かる」と「取れる」は違う。

```
正解 かつ 反応時間 < しきい値  → box + 1
正解 だが 反応時間 ≧ しきい値  → box 据え置き（「おしい！」と表示）
誤答                          → box = 0
```

**しきい値は 4秒／5秒／7秒 から選ぶ。既定は5秒。**

⚠️ 当初3秒にしていたが、**本物の朗詠を入れたら3秒では決まり字まで読まれない**（晴の指摘）。
LibriVoxの通し読みは1首7秒、朗詠は24秒。節回しがあるぶん、決まり字に届くまでが遅い。
**音源を替えたら、この値も見直すこと。**旧設定(2秒/3秒)が残っている端末は5秒に寄せる。

### 7.3 出題の選び方

1セッション20問の内訳：

- **未出題の札を最優先**（まだ一度も見ていないもの）
- 次に box の低い順。box 0 は必ず入れる
- 「復習期限が来た札」を混ぜる
- 直前のセッションで出た札は、box 0 でなければ連続では出さない
- 同じ札が1セッションに出るのは最大2回まで

**にがてだけモード**：box 0〜1 の札と、直近5回の正答率が6割未満の札だけを集める。

### 7.4 記録の保険

iOS のストレージは消える可能性がある。`せいせき › 記録を書き出す` で JSON をダウンロードできるようにする。
読み込みもできるようにして、iPad を替えたときに引き継げるようにする。

---

## 8. UI・見た目

- **対象は小4**。漢字にはふりがな（デフォルトON、設定で消せる）
- **札の大きさと並びは `layoutBoard()` が実寸で決める。**
  ⚠️ **CSSの `aspect-ratio` に任せてはいけない。** `height:100%` や `max-width:100%` と
  組み合わさると、CSSは縦を優先して**比率を捨てる**。4枚のとき 243×532（比0.46）の
  短冊になった。列数は「札がいちばん大きくなる並び」を、枚数を割り切れる候補から選ぶ。
  実測：4枚→242×344・4列／8枚→180×256・4列／16枚→117×166・8列。いずれも比0.70
- **札は実物の比率（53:73）の縦書き**。下の句の改行は**実物（IMG_1092〜1096）に合わせて
  3行・上2行が5文字・3行目が残り**（14字→5/5/4、15字→5/5/5、16字→5/5/6）。
  ⚠️ **句の切れ目では割らない。**実物がそうなっていない。最初「七七で割る」と思い込んで作り直した。
  **札の見た目そのものが記憶の手がかり**なので、ここは実物に従う。データは `shimonoku_lines`
- **文字は上詰め**。中央揃えにすると札ごとに1文字目の高さが変わり、読むたびに目線が動く
- **盤の札は全部おなじ文字サイズ**にする。札ごとに最適化すると、これも1文字目の位置がずれる。
  いちばん厳しい札（3行目が6文字の21番など）に全体を合わせる
- **文字の大きさは札の実寸から計算する**（`fitFuda()`）。ビューポート基準（vh）にすると
  iPadでも小さすぎた（実測 17px）
- ⚠️ **札の実寸が異常に小さいときは測らない。** ホーム画面追加の直後やタブが裏にあるとき
  ビューポートが0×0で返り、2pxのまま焼き付く（実際に踏んだ）。40px未満なら見送って、
  ResizeObserver の次の通知でやり直す
- **五色の縁は札の幅の5%の太さ**。1pxでは色分けが判別できない。
  ⚠️ 縁の太さを先に確定してから文字を測ること。順序を逆にすると中身の幅が減って列が増え、横にはみ出す（踏んだ）
- 計算だけに頼らず、**実測ではみ出さなくなるまで詰める**（折り返しが1文字ずれるだけで列数が変わるため）
- **盤面のサイズ変化を ResizeObserver で監視して測り直す。** 決まり字クイズは上の句が1文字ずつ
  伸びるので途中で盤面が縮む。最初に測った寸法のままだと札からあふれて4行になる（踏んだ）

#### 8.0 ホーム画面のレイアウト（iPad実機で直したこと）

- ⚠️ **ヘッダを絶対配置にしない。** すやり霞の金線と、下の「いろを えらぶ」の行が
  重なって読めなくなった。ふつうに上から流す
- ⚠️ **モードのボタンは名前が折り返さない幅を確保する。**`flex:1 1 12rem` で横に並べると
  6つで狭くなり、「はじめて おぼえる」の「る」だけが2行目に落ちた。
  `grid-template-columns:repeat(auto-fit,minmax(15rem,1fr))` にして、名前は `white-space:nowrap`

## 8.1 世界観

「わくわくしない」と言われて作り直した。手がかりは**漆の箱と毛氈**。

| 要素 | 決めごと |
|---|---|
| 地色 | 漆黒に近い紺 `#141824`。**砂子**（金箔を撒いた料紙のちらつき）をCSSで散らす |
| 金 | `#c9a227` / 淡金 `#e6cd7e`。表題・すやり霞の帯・進捗バー・決まり字の光り |
| 蘇芳 | `#9e3d3d`。落款（朱の角印「歌」）と、札の上の決まり字 |
| 料紙 | `#f7f0de`。札の地。暗い地に対して高いコントラストが出る |
| **毛氈** | `#3d1f28`。**競技かるたは毛氈の上で取る**ので、盤面を毛氈にした。料紙の札が映える |
| 表題 | 「ふだっち」を金のグラデーションで大きく、脇に落款、下にすやり霞の帯 |
| 色えらび | ボタンではなく**縦書きの小さな札**そのものにした。選ぶと札が持ち上がる |
- 色モードでは、その色を画面の縁や札の帯に使う。**色そのものが記憶の手がかりになる**ので、はっきり出す
- タップ領域は最低44pt。実際は札が大きいので余裕がある
- 正解の演出は短く（0.3秒）。テンポが命。派手なアニメーションは入れない
- 音が出せない場所でも使えるよう、モード③④は完全に無音で成立させる
- ダークモード対応（寝る前に使うので）

**アプリの名前は「ふだっち」**（なつさんが決定・2026-08-31）。札＋タッチ。
`<title>`・PWAのホーム画面名・ホーム画面のロゴに使う。設定で変えられるようにはしておく。

---

## 9. ファイル構成

```
hyakunin-oboe/
├── index.html          … 単一ページ。画面はJSで切り替える
├── app.js              … 本体
├── style.css
├── sw.js               … Service Worker（オフライン）
├── manifest.json       … PWA設定
├── icons/              … ホーム画面アイコン
├── data/
│   ├── poems.json      … 100首＋算出済み決まり字
│   └── goshoku.json    … 五色の色分け（実物カードから）
├── tools/
│   ├── build-kimariji.mjs   … 決まり字を算出して poems.json に焼く
│   └── verify.mjs           … §3.2 の4つの検証テスト
├── README.md
└── .gitignore          … audio/ を除外（音源を誤ってコミットしない保険）
```

---

## 10. 進め方

### v0.1 データ ✅ 完了（2026-08-31）
- [x] 100首のかな表記を整備（`tools/poems.src.tsv`）
- [x] 決まり字を算出、検証テスト8本すべてパス（`tools/build.mjs`）
- [x] 五色データを実物カードから読み取り、文化ネットと全100首照合（`tools/crosscheck.mjs`）
- [x] `data/poems.json` `data/goshoku.json` 出力済み

### v1.0 アプリ ✅ 完了（2026-08-31）
- [x] 4モード実装（札取り・決まり字クイズ・上の句→下の句・暗唱チェック）
- [x] ライトナー箱＋速さ判定の出題（`srs.js`。単体テスト25本 `tools/test-srs.mjs`）
- [x] 音源3層（取り込み → LibriVox同梱 → 合成音声）
- [x] LibriVox音源を100首に分割し同梱（5.4MB。`tools/split-audio.py`）
- [x] PWA化（manifest・Service Worker・アイコン4種）
- [x] せいせき画面・せってい画面・記録の書き出し／読みこみ
- [x] ブラウザで4モードすべて動作確認
- [ ] **iPadの実機確認**（未実施。ホーム画面追加とオフライン動作はまだ試していない）

### v1.1 音源 ✅ 前倒しで完了
- [x] 取り込みUI、IndexedDB保存
- [x] 音源チェック画面（100首を鳴らして歌とのずれを確かめる）

### v1.2 その後
- [x] 記録のエクスポート／インポート
- [x] **使う人の切り替え**（なつ・晴・…）。ホーム右上のボタンから切り替え／追加。
      記録は `fudacchi.progress.<名前>` に分かれ、互いに影響しない。設定から削除もできる。
      ⚠️ 設定（札の枚数・しきい値など）は**人ごとではなく端末共通**。分けたくなったら要変更
- [ ] 連続読み（前の下の句→次の上の句）

### 実装中に見つけたこと（記録）

**単体テストが設計バグを2件捕まえた。** どちらも動かしただけでは絶対に気づかない類い。

1. **いちばん迷う札が3回に1回しか出ていなかった。** 誤答の札を「決まり字が近い順に上位21枚集めて
   シャッフルして7枚取る」実装だったため、31番の相方である64番「あさぼらけう」が
   1/3の確率でしか盤に載らない。§6の「本番で迷う組を意図的にぶつける」が機能していなかった。
   → **頭の読みが最も長く一致する札は必ず載せ、残りだけをランダムに埋める**ように変更
2. 比較の基準を決まり字ではなく**上の句全体の読み**に変更。決まり字より先の一致まで見えるので、
   紛らわしさの序列が正しくつく

**決まり字の算出でも2回踏んだ**（§3.2）。26番「をぐらやま」の を、44番「あふことの」の長音。
いずれも検証テストが落ちて気づいた。**この種の誤りは、目視では出ない。**

### なお先生の宿題

**A. 五色のカードを撮る** ✅ 完了（2026-08-31。IMG_1092〜1096）

**B. 本物の読み方の音源を入手する**（v1.1 まで。急がない）
LibriVoxのPD音源を同梱するので、無くてもアプリは動く。ただし「独特の読み方」は入手が要る。
[ogura100.roudokus.com](https://ogura100.roudokus.com/DL/) がメール登録で全100首＋序歌のmp3。メルマガが届く。
**入手した音源はリポジトリに置かない。** iPad から取り込む。

**C. リポジトリ名** ✅ `hyakunin-oboe` に決定

---

## 11. やらないこと

- 対戦機能・通信対戦（一人で覚えるための道具に絞る）
- ログイン・アカウント（記録は端末内のみ。子どものデータを外に出さない）
- 広告・課金・アクセス解析
- 歌の意味や背景の解説（覚える邪魔になる。必要なら後日、別モードで）
- 変体仮名の表示（競技かるたの札は現代の字体。読めなくてよい）

---

## 12. 未確定・確認したいこと

| # | 事項 | 状態 |
|---|---|---|
| 1 | 五色の色分けデータ | ✅ 確定（実物カード＋文化ネットで二重に確認） |
| 2 | iOS PWA のストレージ保持期間 | **未検証**。エクスポート機能で保険をかける |
| 3 | 反応時間の閾値3.0秒 | 暫定。実使用で調整 |
| 4 | 音源のファイル名規則 | 入手してから確認。取り込みUIは推定＋手動修正の両対応にする |
| 5 | 学校で最初に使う色 | 未定（まだ授業が始まっていない）。**初期値は「赤（桃札）」**とし、設定で変えられるようにする |
| 6 | アプリの名前 | 候補6案を提示済み（§8）。なつさんが決める |
| 7 | ふりがな（ルビ）の整備 | 見送った。漢字の横に**かな全文を添える**方式にしたので、語単位のルビ対応データを作らずに済んでいる。真のルビが要るなら別途 |
| 9 | **LibriVox音源と歌の対応** | 構造的には検証ずみ（各ファイル10区間・長さも範囲内）だが、**中身は聴いて確かめていない**。`せってい › 音源を たしかめる` で数首（1番・51番・91番）を鳴らして、歌と音が合っているか確認してほしい。分割の例外処理が入っているのがこの3ファイル |
| 10 | iPad実機での確認 | 未実施。公開ずみなのでiPadのSafariで開いて確認できる |
| 13 | 札の裏の行割り | 実物の裏を見ていないので、3行の均等割りにしてある。裏の写真があれば合わせる |
| 12 | 「せいかいで音をとめる＝とめない」の実動確認 | ブラウザ自動操作では正解を引けず未確認。実装は入れた（音が鳴っている間に取ると「さいごまで きいてね」が出る） |
| 11 | 音源配布元のファイル名 | ✅ 解決（下記 §4.6） |
| 8 | 上の句・下の句の漢字表記 | 資料により異同あり（例「陸奥／みちのく」）。かな表記は検証済みだが、漢字表記は未照合。実物カード（読み札）に寄せる方針 |
